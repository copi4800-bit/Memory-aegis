# Plan

1. Define a timeline payload that uses real governance events and memory state transitions.
2. Implement a dedicated surface/report plus demo/render scripts.
3. Wire the timeline into app, MCP, registry, plugin metadata, workflow shell, and dashboard shell.
4. Add regression tests and run targeted verification.
