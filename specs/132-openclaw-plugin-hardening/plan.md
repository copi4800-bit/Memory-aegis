# Plan

1. Add stable `truthkeep-setup`, `truthkeep-check`, and `truthkeep-mcp` entrypoints at the Python package layer.
2. Add npm `bin` mappings and `openclaw.extensions` metadata for host/plugin discovery.
3. Update README and QUICKSTART to document the actual public commands.
4. Add tests that assert packaging and manifest consistency.
5. Run smoke checks and the full test suite.
